---
import type { Game } from '../types/game';
import Tag from './Tag.astro';
import ArrowRight from '../assets/icons/arrow-right.svg';

interface Props {
    game: Game;
}

const { game } = Astro.props;
---

<a
    href={`/game/${game.id}`}
    class="group block bg-slate-800/60 high-contrast:bg-black backdrop-blur-sm rounded-xl overflow-hidden shadow-lg border border-slate-700/50 high-contrast:border-white hover:border-blue-500/50 high-contrast:hover:border-yellow-300 hover:shadow-blue-500/10 hover:shadow-xl transition-all duration-300 motion-reduce:transition-none hover:translate-y-[-6px] motion-reduce:hover:translate-y-0 focus:outline-none focus:ring-2 focus:ring-blue-500 high-contrast:focus:ring-yellow-300"
    data-testid="game-card"
    data-game-id={game.id}
    data-game-title={game.title}
>
    <div class="p-6 relative">
        <div class="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-purple-600/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        <div class="relative z-10">
            <h3 class="text-xl font-semibold text-slate-100 high-contrast:text-white mb-2 group-hover:text-blue-400 high-contrast:group-hover:text-yellow-300 transition-colors motion-reduce:transition-none" data-testid="game-title">{game.title}</h3>

            {(game.category?.name || game.publisher?.name) && (
                <div class="flex gap-2 mb-3">
                    {game.category?.name && (
                        <Tag color="blue" data-testid="game-category">
                            {game.category.name}
                        </Tag>
                    )}
                    {game.publisher?.name && (
                        <Tag color="purple" data-testid="game-publisher">
                            {game.publisher.name}
                        </Tag>
                    )}
                </div>
            )}

            <p class="text-slate-400 high-contrast:text-white mb-4 text-sm line-clamp-2" data-testid="game-description">{game.description}</p>

            <div class="mt-4 text-sm text-blue-400 high-contrast:text-yellow-300 font-medium flex items-center">
                <span>View details</span>
                <ArrowRight class="h-4 w-4 ml-1 transform transition-transform duration-300 group-hover:translate-x-2" aria-hidden="true" />
            </div>
        </div>
    </div>
</a>
